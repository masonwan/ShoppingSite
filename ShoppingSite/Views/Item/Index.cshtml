@using ShoppingSite.Models
@using ShoppingSite.Controllers;
@model Item
@{
    ViewBag.Title = "Item";
    var item = Model;
    var categories = ViewData["categories"] as List<Category>;
}
<div id="content">
    <aside class="white-box-top">
    </aside>
    <aside class="white-box-middle">
        @Html.Sidebar(Url, categories, item.DepartmentId);
        <aside class="pro-right">
            <img alt="" src="@Url.GetImageUrl("shirts.jpg")" />
            <div class="clear">
                &nbsp;</div>
            <br />
            <aside class="pro-view">
                <img alt="" src="@Url.GetItemImageUrl(item)" style="width:265px;" class="left brdr" />
                <a href="#" class="left">
                    <img alt="" src="@Url.GetImageUrl("ico-zoom.jpg")" align="absmiddle" />
                    Roll to Zoom In</a> <a href="#" class="right">
                        <img alt="" src="@Url.GetImageUrl("ico-view.jpg")" align="absmiddle" />
                        View Enlarge</a>
                <div class="clear">
                    &nbsp;</div>
                <form id="form" action="@Url.Action("SetItems", "Cart")" method="post">
                <p class="left">
                    <span class="mart-5 left">Qty:&nbsp;&nbsp;</span>
                    <select name="Quantity" class="input6 left" form="form">
                        <option>1</option>
                        <option>2</option>
                        <option>3</option>
                        <option>4</option>
                        <option>5</option>
                        <option>6</option>
                        <option>7</option>
                        <option>8</option>
                        <option>9</option>
                    </select>
                </p>
                <p class="left marlr-10">
                    <input type="submit" value="" style="background-image:url('@Url.GetImageUrl("add-cart.jpg")'); width:110px; height:28px;" /></p>
                    <input type="hidden" name="ItemId" value="@item.Id" />
                </form>
            </aside>
            <aside class="pro-info">
                <h1>@item.Name</h1>
                <aside class="left txt-aln-r marl-30">
                    <span class="mart-10 left">Price:&nbsp;&nbsp;&nbsp;&nbsp;</span> <strong class="red txt17 left mart-10">@String.Format("{0:C}", item.Price)</strong>
                </aside>
                <div class="clear">
                    &nbsp;</div>
                <h2>
                    About this product</h2>
                <p>@Html.Raw(item.Description.Replace("\\n", "<br />"))</p>
            </aside>
            <div class="clear">
                &nbsp;</div>
            <br />
            <br />
        </aside>
    </aside>
    <aside class="white-box-bottom">
    </aside>
    <div class="clear">
    </div>
</div>
